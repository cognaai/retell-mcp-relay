{
  "name": "retell-mcp-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Auth-gated relay that forwards MCP requests to Retell's hosted MCP server with a fixed API key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  }
}
